# Plan 10.2: Remaining Narrative UX Follow-ups

## Goal

Close the remaining player-facing follow-ups around discoverability, choice-stat comprehension, and local developer builds.

## Tasks

1. Add in-context choice stat inspect/help via keyboard from the narrative view.
2. Improve help text around footer telemetry and session/save controls where needed.
3. Add a reliable local build path that refreshes the repo-root `./oneday` binary, not just temporary/cross-platform artifacts.
4. Update docs where needed so local testing and save/session behavior are obvious.

## Verification

- players can inspect choice-related stats without leaving the narrative flow
- help text matches the actual shortcuts and footer meanings
- `go build -o ./oneday ./cmd/oneday` is the documented and verified local-root build path
