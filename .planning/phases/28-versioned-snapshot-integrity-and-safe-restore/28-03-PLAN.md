---
phase: 28-versioned-snapshot-integrity-and-safe-restore
plan: 28-03
wave: 3
autonomous: true
requirements: [SNAP-03, SNAP-04]
files_modified:
  - internal/engine/save.go
  - internal/engine/save_test.go
  - internal/storage/saves.go
must_haves:
  truths:
    - "Session mirrors are staged and validated before destructive canonical restore."
    - "A file-stage or swap failure is compensated and cannot leave a successful-looking mixed state."
    - "The final round-trip matrix covers every aggregate promised by the snapshot manifest."
---

# Plan 28.3: Staged Session Restore and Exhaustive Rollback Matrix

## Objective

Make DB and session-file restoration a compensated flow and close the phase with
fault-injection plus exhaustive round-trip coverage.

## Tasks

1. Add filesystem fault-injection seams and failing tests for staging, validation, DB commit, swap, and compensation failures.
2. Stage session files in a story-local temporary directory, validate paths/content, and prepare an atomic swap/backup operation before DB mutation.
3. Coordinate DB rollback and file compensation so failures preserve or recover the pre-load state and temporary artifacts are cleaned safely.
4. Extend the public save/load fixture and migration matrix to assert every required manifest collection and session mirror.
5. Run full Go, Rust gateway, format, and existing release-safe tests; document any platform-specific atomic-rename constraint.

## Acceptance

- SNAP-03 and SNAP-04 pass under happy-path and injected failure tests.
- No failure path reports success while DB and files describe different timelines.
- Full project regression tests remain green.

## Verification

- `go test ./... -count=1`
- `cargo test --manifest-path gateway/Cargo.toml`

