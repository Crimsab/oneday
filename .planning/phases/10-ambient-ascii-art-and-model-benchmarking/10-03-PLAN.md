# Plan 10.3: ASCII Benchmark and Provider Alignment

## Goal

Benchmark dedicated ASCII-art models and align OneDay's local provider setup with the chosen runtime defaults.

## Tasks

1. Extend `cmd/oneday-benchmark` with an ASCII-focused benchmark mode and report output under `docs/benchmarks/`.
2. Benchmark these models:
   - `google/gemma-4-31b-it:free`
   - `google/gemma-4-26b-a4b-it:free`
   - `x-ai/grok-4.1-fast`
   - `google/gemini-2.5-flash-lite`
   - `z-ai/glm-4.5-air:free`
   - `minimax/minimax-m2.5:free`
   - `openai/gpt-oss-120b:free`
   - `nvidia/nemotron-3-super-120b-a12b:free`
3. Include latency, completion time, throughput, cost, context window, and OpenRouter catalog metadata in the report.
4. Record qualitative notes for ASCII usefulness/readability.
5. Align OneDay's local config and LiteLLM expectations with:
   - main narrator model
   - fallback model
   - embedding model
   - selected ASCII model

## Verification

- benchmark command runs and writes JSON + Markdown reports under `docs/benchmarks/`
- report includes multiple ranking views, not just one raw score
- local config/proxy expectations no longer reference unavailable models
