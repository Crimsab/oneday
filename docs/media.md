# Generated media

OneDay can add images, map art, ambient ASCII, and spoken audio to a story. All
media is optional, asynchronous, and subordinate to committed text and state.
The public template leaves automatic visual generation disabled until a
provider is configured.

## Images with imagegen-bridge

[`imagegen-bridge`](https://github.com/Crimsab/imagegen-bridge) is the preferred
adapter. It gives OneDay one native contract while the bridge owns provider
authentication, capability normalization, ordered fallback, revised prompts,
idempotency, and output metadata.

The default route is `codex-responses` with
`codex-app-server:gpt-image-2` as the technical-error fallback. These routes use
Codex/ChatGPT OAuth, not `OPENAI_API_KEY`.

```yaml
ai:
  image_generation:
    provider: imagegen-bridge
    imagegen_bridge_url: http://127.0.0.1:8787
    imagegen_bridge_token: ${ONEDAY_IMAGEGEN_BRIDGE_TOKEN}
    imagegen_bridge_provider: codex-responses
    imagegen_bridge_fallbacks: [codex-app-server:gpt-image-2]
    imagegen_bridge_fallback_policy: on_error
    imagegen_bridge_compatibility: normalize
    auto_generate: true
```

Use `http://host.docker.internal:8787` when OneDay runs in Docker and the bridge
runs on the host. Keep the bearer token in `.env`.

## Other image endpoints

Set `provider: openclaw-bridge` to call the legacy bridge URL. Any other
provider name uses `base_url`, bearer `api_key`, and the OpenAI-compatible
`/images/generations` contract. LiteLLM and external providers work through
that route only when they implement the same path and payload; otherwise place
their adapter in imagegen-bridge.

General art and map symbols can use independent provider/model routes. Size,
aspect ratio, resolution, quality, format, background, and timeouts can be set
globally or by asset kind. The story wizard saves a visual direction and
negative guidance to reduce drift across later assets.

## Spoken audio

TTS supports an OpenAI-compatible cloud endpoint and a local Piper-compatible
service. Both are disabled until explicitly enabled.

```yaml
ai:
  tts:
    output_dir: ./oneday_data/audio
    timeout_seconds: 45
    provider_order: [local, cloud]
    cloud:
      enabled: false
      base_url: https://api.openai.com/v1
      api_key: ${ONEDAY_TTS_API_KEY}
      model: gpt-4o-mini-tts
      voice: alloy
      version: "1"
      languages: [en, it]
    local:
      enabled: false
      base_url: http://127.0.0.1:5000
      model: piper
      voice: ""
      version: "1"
      languages: [en, it]
```

The cloud adapter calls `/audio/speech`. The local adapter expects `/voices`
and `/synthesize`. Story-level voice profiles select language, voice, style,
and provider. Output is cached by committed text and voice settings, and jobs
can be retried or cancelled without changing story canon.

## Ambient ASCII

`ai.ascii_art` controls optional scene headers, signage, and terminal ambience.
It uses a text model and remains decorative; failures do not block a turn.

## Failure, privacy, and traces

Media failures record bounded status and lineage, then leave the text experience
usable. API keys, bridge tokens, prompts, story text, and generated bytes are
not exported in OpenTelemetry spans. Back up the whole data directory to retain
generated assets together with their SQLite records.

See [Configuration](configuration.md) for all fields and [Troubleshooting](troubleshooting.md)
for provider diagnostics.
