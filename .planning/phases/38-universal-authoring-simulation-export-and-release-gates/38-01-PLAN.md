# Plan 38.1

Emit deterministic, bounded, cooldown-aware NPC offscreen actions as immutable
canonical events inside turn commits, expose active-branch event history, and
add a player-safe audit feed without leaking private NPC state.
