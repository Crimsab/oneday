---
phase: 24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/aifactory/factory.go
  - internal/ai/provider.go
  - config.example.yaml
requirements:
  - AI-SETUP-01
  - AI-SETUP-03
autonomous: true
must_haves:
  truths:
    - "Config has explicit model slots for primary generation, utility/repair, ASCII, and embeddings."
    - "Codex defaults use gpt-5.5 for narrator generation and gpt-5.4-mini for utility/repair calls."
    - "Codex remains generation-only and is never considered embedding-capable."
  artifacts:
    - path: "internal/config/config.go"
      provides: "AI config schema, defaults, validation, and repair model candidate ordering"
    - path: "internal/aifactory/factory.go"
      provides: "provider construction from normalized config"
    - path: "config.example.yaml"
      provides: "tracked friend-safe example schema"
  key_links:
    - from: "internal/config/config.go"
      to: "config.example.yaml"
      via: "matching YAML keys and defaults"
      pattern: "utility|repair_model|gpt-5.4-mini|text-embedding-3-small"
---

<objective>
Normalize the AI provider configuration schema so primary generation, utility/repair, ASCII, and embedding model routing are explicit across Codex, LiteLLM, and OpenRouter.

Purpose: later setup, doctor, and runtime work need one canonical contract instead of scattered model defaults.
Output: updated config schema/defaults, validation, factory consumption, and example YAML.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
@.planning/phases/10-ambient-ascii-art-and-model-benchmarking/10-03-SUMMARY.md
@.planning/phases/12-state-rollback-integrity-and-narrative-persistence/12-03-SUMMARY.md
@.planning/phases/16-streaming-memory-reliability-and-provider-capability-hardening/16-03-SUMMARY.md
@internal/config/config.go
@internal/aifactory/factory.go
@internal/ai/provider.go
@config.example.yaml

<interfaces>
Current config contracts:
```go
type AIConfig struct {
    ProviderPriority []string `yaml:"provider_priority"`
    Codex CodexConfig `yaml:"codex"`
    ClaudeCode ClaudeCodeConfig `yaml:"claude_code"`
    LiteLLM LiteLLMConfig `yaml:"litellm"`
    OpenRouter OpenRouterConfig `yaml:"openrouter"`
    Embedding EmbeddingConfig `yaml:"embedding"`
    ASCIIArt ASCIIArtConfig `yaml:"ascii_art"`
    Generation GenerationConfig `yaml:"generation"`
}
```

Important locked decisions from 24-CONTEXT:
- Codex CLI is generation-only, not embedding-capable.
- Codex primary narrator default is `gpt-5.5`.
- Codex utility/repair/ancillary default is `gpt-5.4-mini`.
- Reasoning values: `off`, `none`, `minimal`, `low`, `medium`, `high`, `xhigh`; `off` maps to CLI `none`.
- Remote embeddings default to `text-embedding-3-small`.
- Secrets must remain env/.env only, not tracked YAML.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider-aware model role config</name>
  <read_first>
    - internal/config/config.go
    - config.example.yaml
    - .planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
  </read_first>
  <files>internal/config/config.go, config.example.yaml</files>
  <behavior>
    - `config.Default()` exposes a utility/ancillary model default of `gpt-5.4-mini` for Codex-compatible repair and small calls.
    - Existing `ai.generation.repair_model` and `repair_fallback_models` continue to load for backward compatibility.
    - `ai.embedding.model` remains `text-embedding-3-small` and `ai.embedding.provider` accepts only `auto`, `litellm`, or `openrouter`.
  </behavior>
  <action>
    Add an explicit utility/ancillary model contract in `internal/config/config.go`, using exact YAML keys under `ai.generation` or a small nested `ai.utility` block. The contract must make these concrete defaults visible: primary Codex narrator `gpt-5.5`, Codex utility/repair/ancillary `gpt-5.4-mini`, embedding model `text-embedding-3-small`, ASCII model `ascii-ambient`. Preserve existing `repair_model` and `repair_fallback_models` behavior by making `GenerationConfig.RepairModelCandidates()` include the new utility default only when no explicit repair model is set, not by breaking old configs. Mirror the schema in `config.example.yaml` with comments that Codex is generation-only and embeddings require LiteLLM/OpenRouter.
  </action>
  <verify>
    <automated>go test ./internal/config -run 'Test|^$'</automated>
    <automated>go test ./... </automated>
    <automated>grep -R "gpt-5.4-mini" internal/config/config.go config.example.yaml</automated>
    <automated>grep -R "text-embedding-3-small" internal/config/config.go config.example.yaml</automated>
  </verify>
  <acceptance_criteria>
    `grep -R "gpt-5.4-mini" internal/config/config.go config.example.yaml` finds the Codex utility default; `grep -R "gpt-5.5" internal/config/config.go config.example.yaml` finds the Codex narrator default; `grep -R "text-embedding-3-small" internal/config/config.go config.example.yaml` finds the embedding default; `go test ./...` passes.
  </acceptance_criteria>
  <done>Codex narrator, utility/repair, ASCII, and embedding defaults are explicit in config code and example YAML, with backward-compatible repair candidate behavior and passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate model roles and provider capabilities</name>
  <read_first>
    - internal/config/config.go
    - internal/ai/provider.go
    - internal/aifactory/factory.go
    - internal/tui/app.go
  </read_first>
  <files>internal/config/config.go, internal/aifactory/factory.go, internal/ai/provider.go</files>
  <behavior>
    - Invalid Codex reasoning values fail validation with a message naming every accepted value.
    - `off` remains accepted and continues mapping to Codex CLI `none` in the provider implementation.
    - No new config path can mark `codex` or `claude-code` as embedding-capable.
  </behavior>
  <action>
    Tighten validation around the normalized role fields added in Task 1. Validate provider names and model-role provider references using concrete provider strings `codex`, `litellm`, `openrouter`, and `claude-code`; reject embeddings configured to `codex` or `claude-code` with a clear error string containing `embedding provider must be one of: auto, litellm, openrouter`. If a role config adds a provider field, make factory/router code ignore it unless it is explicitly used by existing request code; do not invent per-story config. Keep `ai.Provider`, `ai.StreamProvider`, and `ai.EmbeddingRequest` signatures backward compatible.
  </action>
  <verify>
    <automated>go test ./internal/config ./internal/aifactory ./internal/ai/... </automated>
    <automated>grep -R "off, none, minimal, low, medium, high, xhigh" internal/config/config.go</automated>
    <automated>grep -R "embedding.provider must be one of: auto, litellm, openrouter" internal/config/config.go</automated>
  </verify>
  <acceptance_criteria>
    Validation error text contains `off, none, minimal, low, medium, high, xhigh`; validation error text for embeddings contains `auto, litellm, openrouter`; no grep for `embedding.*codex` in `config.example.yaml` presents Codex as embedding-capable; targeted package tests pass.
  </acceptance_criteria>
  <done>Config validation enforces reasoning/model-role/provider constraints, preserves provider interfaces, and prevents Codex/Claude Code from being embedding providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml/.env to runtime | User-controlled local config and environment values enter provider routing and HTTP/CLI calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | `internal/config/config.go` | mitigate | Validate provider names, reasoning values, positive token/time fields, and embedding provider allowlist before runtime use. |
| T-24-01-02 | Information Disclosure | `config.example.yaml` | mitigate | Keep API keys as `${ONEDAY_*}` placeholders only; do not add real secrets or local paths. |
</threat_model>

<verification>
Run `go test ./...` and grep for the exact defaults and validation strings listed in task acceptance criteria.
</verification>

<success_criteria>
AI config can represent primary, utility/repair, ASCII, and embedding models explicitly; Codex defaults are `gpt-5.5` and `gpt-5.4-mini`; embeddings remain remote-only through LiteLLM/OpenRouter.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-01-SUMMARY.md`
</output>
