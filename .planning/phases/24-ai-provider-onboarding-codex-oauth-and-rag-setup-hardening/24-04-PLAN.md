---
phase: 24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 24-02
  - 24-03
files_modified:
  - internal/ai/providers/openai_compat.go
  - internal/ai/providers/codex.go
  - README.md
  - .env.example
  - .gitignore
  - Makefile
requirements:
  - AI-SETUP-04
  - DIST-01
  - RAG-SETUP-01
autonomous: true
must_haves:
  truths:
    - "LiteLLM/OpenRouter auth failures name the missing env var or disabled provider."
    - "Codex failures explain CLI missing/login/setup issues instead of raw exec noise."
    - "README and examples provide a friend-safe handoff path."
    - "Ignored files cover local config, secrets, data, binaries, archives, and databases."
  artifacts:
    - path: "internal/ai/providers/openai_compat.go"
      provides: "actionable OpenAI-compatible provider errors"
    - path: "internal/ai/providers/codex.go"
      provides: "actionable Codex CLI errors"
    - path: "README.md"
      provides: "setup, doctor, Codex OAuth, RAG, and release handoff docs"
    - path: ".gitignore"
      provides: "friend-safe exclusion policy"
    - path: "Makefile"
      provides: "verification/release helper surface"
  key_links:
    - from: "README.md"
      to: "cmd/oneday/main.go"
      via: "documents `oneday setup` and `oneday doctor` commands implemented in prior plans"
      pattern: "oneday setup|oneday doctor"
---

<objective>
Improve provider error handling, docs, and friend-safe release packaging after the schema, setup, doctor, and RAG hardening are in place.

Purpose: handoff users need actionable failures and safe distribution artifacts, not raw provider errors or accidental local files.
Output: translated provider errors, Codex guidance, updated docs/examples, and release packaging hygiene checks.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
@internal/ai/providers/openai_compat.go
@internal/ai/providers/codex.go
@cmd/oneday/main.go
@config.example.yaml
@.env.example
@.gitignore
@README.md
@Makefile

<interfaces>
Existing provider errors:
```go
return ai.Response{}, fmt.Errorf("HTTP request to %s: %w", o.name, err)
return ai.Response{}, fmt.Errorf("codex CLI exec: %w: %s", err, strings.TrimSpace(string(output)))
```

Required actionable env var names:
- `ONEDAY_LITELLM_API_KEY`
- `ONEDAY_OPENROUTER_API_KEY`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate provider authentication and CLI failures</name>
  <read_first>
    - internal/ai/providers/openai_compat.go
    - internal/ai/providers/codex.go
    - internal/ai/provider.go
    - cmd/oneday/main.go
  </read_first>
  <files>internal/ai/providers/openai_compat.go, internal/ai/providers/codex.go</files>
  <behavior>
    - LiteLLM 401 or missing key errors mention `ONEDAY_LITELLM_API_KEY`.
    - OpenRouter 401 or missing key errors mention `ONEDAY_OPENROUTER_API_KEY`.
    - Codex CLI missing or unauthenticated errors mention `codex login` and `oneday doctor`.
    - Error bodies are summarized without leaking full secrets or excessive upstream payloads.
  </behavior>
  <action>
    Add provider-specific error translation in `internal/ai/providers/openai_compat.go` for chat, stream, and embedding paths. For provider `litellm`, empty API key or HTTP 401/403 must return an error containing `LiteLLM authentication failed` and `ONEDAY_LITELLM_API_KEY`; for provider `openrouter`, use `OpenRouter authentication failed` and `ONEDAY_OPENROUTER_API_KEY`. Preserve status code and a short sanitized body snippet. In `internal/ai/providers/codex.go`, detect `exec.ErrNotFound` and common login/auth failure output; return errors containing `Codex CLI not found`, `codex login`, or `run oneday doctor` as appropriate. Do not change provider interfaces unless required by tests.
  </action>
  <verify>
    <automated>go test ./internal/ai/providers</automated>
    <automated>grep -R "ONEDAY_LITELLM_API_KEY\\|ONEDAY_OPENROUTER_API_KEY" internal/ai/providers</automated>
    <automated>grep -R "Codex CLI not found\\|codex login\\|oneday doctor" internal/ai/providers/codex.go</automated>
  </verify>
  <acceptance_criteria>
    Provider tests assert 401/missing-key translation for LiteLLM and OpenRouter; Codex tests or source checks cover missing CLI/login guidance; grep finds both env var names in provider code; targeted tests pass.
  </acceptance_criteria>
  <done>OpenAI-compatible and Codex provider failures are translated into actionable errors that name the right env vars or setup commands without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Update handoff docs and examples</name>
  <read_first>
    - README.md
    - config.example.yaml
    - .env.example
    - .gitignore
    - Makefile
    - .planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
  </read_first>
  <files>README.md, .env.example, .gitignore, Makefile</files>
  <action>
    Update `README.md` Quick Start and Configuration sections to make `go run ./cmd/oneday setup`, `go run ./cmd/oneday doctor`, Codex OAuth via `codex login`, Codex defaults `gpt-5.5` and `gpt-5.4-mini`, env-var secret storage, and RAG embedding behavior explicit. Add a short Friend-Safe Sharing section naming `config.example.yaml`, `.env.example`, `config.yaml`, `.env`, `oneday_data/`, binaries, release archives, and databases. Update `.env.example` comments so keys are documented but empty. Confirm `.gitignore` covers `config.yaml`, `.env`, `oneday_data/`, `/oneday`, `/oneday.exe`, `/build/`, `dist/`, `release/`, `*.tar.gz`, `*.zip`, `*.db`, `*.sqlite`, and `*.sqlite3`; add missing patterns only. If helpful, add a `doctor` phony target to `Makefile` that runs `go run ./cmd/oneday doctor`.
  </action>
  <verify>
    <automated>grep -R "oneday doctor\\|Codex OAuth\\|gpt-5.5\\|gpt-5.4-mini\\|text-embedding-3-small\\|Friend-Safe" README.md</automated>
    <automated>grep -E "config.yaml|\\.env|oneday_data|/oneday|/build/|dist/|release/|\\.tar\\.gz|\\.zip|\\.db|\\.sqlite" .gitignore</automated>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    README contains `oneday doctor`, `Codex OAuth`, `gpt-5.5`, `gpt-5.4-mini`, `text-embedding-3-small`, and `Friend-Safe`; `.env.example` contains no non-empty secret values; `.gitignore` contains all local config/secret/data/binary/archive/database patterns; full tests pass.
  </acceptance_criteria>
  <done>README, env examples, ignore rules, and optional Makefile helpers document the safe first-time setup and handoff workflow with no secret-bearing examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add friend-safe release hygiene check</name>
  <read_first>
    - Makefile
    - scripts/release-gate.sh
    - .gitignore
    - README.md
  </read_first>
  <files>Makefile, scripts/release-gate.sh</files>
  <behavior>
    - Release checks fail if a packaged/staged artifact includes `config.yaml`, `.env`, `oneday_data`, local binaries, archives, or SQLite databases outside intended build output.
    - `make release-check` still runs the existing verification flow.
  </behavior>
  <action>
    Inspect `scripts/release-gate.sh` before editing. Extend it or add a small helper script invoked by `release-check` to verify friend-safe hygiene with grep/find checks against the repo tree. The check must print exact string `friend-safe release hygiene` and fail if tracked or package-source files include forbidden local artifacts: `config.yaml`, `.env`, `oneday_data`, `*.db`, `*.sqlite`, `*.sqlite3`, root `oneday`, root `oneday.exe`, `release/`, or `dist/`. Do not block expected ignored working-tree files when they are untracked local runtime files; focus on tracked files and release package inputs.
  </action>
  <verify>
    <automated>make release-check</automated>
    <automated>grep -R "friend-safe release hygiene" Makefile scripts</automated>
  </verify>
  <acceptance_criteria>
    `make release-check` runs successfully; grep finds `friend-safe release hygiene` in Makefile or scripts; release hygiene fails on tracked forbidden artifacts but does not require deleting ignored local `config.yaml`, `.env`, `oneday_data/`, or binaries from the developer machine.
  </acceptance_criteria>
  <done>Release checks include friend-safe hygiene validation while preserving ignored local runtime files on the developer machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider HTTP/CLI errors to user | Upstream error bodies and CLI output are transformed into local diagnostic messages. |
| local repo to shared release | Local runtime files must not enter tracked docs/examples or release packaging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Information Disclosure | provider error translation | mitigate | Sanitize/truncate upstream bodies and never print API key values. |
| T-24-04-02 | Information Disclosure | release artifacts | mitigate | Add release hygiene checks for config, `.env`, data dirs, binaries, archives, and databases. |
| T-24-04-03 | Repudiation | diagnostics/docs | mitigate | Doctor/docs name exact env vars and setup commands so failure causes are auditable. |
</threat_model>

<verification>
Run `go test ./...` and `make release-check`; grep README, provider code, `.gitignore`, and scripts for the exact strings in acceptance criteria.
</verification>

<success_criteria>
Provider failures are actionable, docs explain first-time setup and RAG behavior, and release/handoff surfaces remain friend-safe by default.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-04-SUMMARY.md`
</output>
