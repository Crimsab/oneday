---
phase: 24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - cmd/oneday/main.go
  - internal/config/dotenv.go
  - internal/aifactory/factory.go
requirements:
  - AI-SETUP-02
  - AI-SETUP-03
  - RAG-SETUP-01
autonomous: true
must_haves:
  truths:
    - "A player can run `oneday setup` and create local config without putting secrets into tracked YAML."
    - "`oneday doctor` is read-only and explains provider, Codex login, model, env, and RAG readiness before gameplay."
    - "Provider smoke tests produce actionable pass/warn/fail output."
  artifacts:
    - path: "cmd/oneday/main.go"
      provides: "setup and doctor CLI commands"
    - path: "internal/config/dotenv.go"
      provides: ".env loading that preserves exported environment variables"
    - path: "internal/aifactory/factory.go"
      provides: "provider construction used by doctor smoke tests"
  key_links:
    - from: "cmd/oneday/main.go"
      to: "internal/aifactory/factory.go"
      via: "doctor builds providers from loaded config"
      pattern: "doctor|NewRouterFromConfig|smoke"
---

<objective>
Complete `oneday setup` and add a read-only `oneday doctor` command with provider and embedding smoke diagnostics.

Purpose: first-time users should discover bad auth, missing keys, Codex login issues, and unusable RAG before starting a story.
Output: CLI setup/doctor flow and reusable diagnostic checks.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
@internal/config/config.go
@internal/config/dotenv.go
@internal/aifactory/factory.go
@internal/ai/provider.go
@internal/ai/providers/codex.go
@internal/ai/providers/openai_compat.go
@cmd/oneday/main.go

<interfaces>
Existing CLI command detection:
```go
func wantsSetup(args []string) bool
func wantsVersion(args []string) bool
func runSetup() error
```

Doctor must be added without mutating `config.yaml`, `.env`, or `oneday_data`.
Required strings for grep/test verification:
- `OneDay doctor`
- `Codex login`
- `Provider smoke`
- `Embedding smoke`
- `RAG: enabled`
- `RAG: disabled`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make setup complete and non-destructive</name>
  <read_first>
    - cmd/oneday/main.go
    - internal/config/dotenv.go
    - config.example.yaml
    - .env.example
    - .planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
  </read_first>
  <files>cmd/oneday/main.go, internal/config/dotenv.go</files>
  <behavior>
    - `go run ./cmd/oneday setup` creates `config.yaml` only when missing.
    - Setup writes provider secrets only to `.env` placeholders and never overwrites existing exported env vars.
    - Codex setup selects priority `codex`, then remote providers as fallbacks, with `gpt-5.5` primary and `gpt-5.4-mini` utility defaults from Plan 24-01.
    - Setup prints a clear RAG warning if the selected provider path has no embedding-capable provider configured.
  </behavior>
  <action>
    Refactor `runSetup()` in `cmd/oneday/main.go` into small helpers for command probes, provider selection, `.env` placeholder writing, and RAG viability reporting. Keep exact provider choices `1) Codex OAuth`, `2) LiteLLM / homelab proxy`, `3) OpenRouter`. For LiteLLM write `ONEDAY_LITELLM_API_KEY=` to `.env` only if neither `.env` nor real `ONEDAY_LITELLM_API_KEY` already exists; for OpenRouter use `ONEDAY_OPENROUTER_API_KEY=`. Do not write API key literals to `config.yaml`; YAML must use `${ONEDAY_LITELLM_API_KEY}` or `${ONEDAY_OPENROUTER_API_KEY}`. For Codex-only setup, leave RAG disabled or warn with exact text containing `RAG: disabled, reason: no embedding-capable provider configured` unless LiteLLM/OpenRouter is also configured for embeddings.
  </action>
  <verify>
    <automated>go test ./cmd/oneday ./internal/config</automated>
    <automated>printf '1\n\n\n' | go run ./cmd/oneday setup</automated>
    <automated>grep -R "RAG: disabled, reason: no embedding-capable provider configured" cmd/oneday/main.go</automated>
    <automated>grep -R 'ONEDAY_LITELLM_API_KEY\\|ONEDAY_OPENROUTER_API_KEY' cmd/oneday/main.go .env.example</automated>
  </verify>
  <acceptance_criteria>
    `cmd/oneday/main.go` contains `RAG: disabled, reason: no embedding-capable provider configured`; setup code contains both `ONEDAY_LITELLM_API_KEY` and `ONEDAY_OPENROUTER_API_KEY`; setup keeps `config.yaml already exists; leaving it in place.` behavior; targeted tests pass.
  </acceptance_criteria>
  <done>`oneday setup` creates non-destructive local config/env scaffolding, keeps secrets out of YAML, and reports RAG viability for the selected provider path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only doctor diagnostics</name>
  <read_first>
    - cmd/oneday/main.go
    - internal/aifactory/factory.go
    - internal/ai/provider.go
    - internal/ai/providers/codex.go
    - internal/ai/providers/openai_compat.go
    - internal/tui/app.go
  </read_first>
  <files>cmd/oneday/main.go, internal/aifactory/factory.go</files>
  <behavior>
    - `go run ./cmd/oneday doctor` prints OS/arch, Go availability, Codex CLI presence, Codex login status, enabled providers, env var state, provider smoke response, embedding smoke response, RAG status, and ignored-file hygiene.
    - Doctor never writes config, `.env`, database, or `oneday_data`.
    - Smoke prompts are tiny and deterministic, e.g. user content `reply with OK`.
  </behavior>
  <action>
    Add `wantsDoctor(args []string) bool` and `runDoctor() error` in `cmd/oneday/main.go`. Call it after version/setup detection and before normal TUI startup. Load `.env` through `config.LoadDotEnv(resolveDotEnvPath())`, load config through `config.Load(resolveConfigPath())`, then print a sectioned report beginning with exact line `OneDay doctor`. Include command checks for `go version`, `codex --version`, `codex login status`, and `claude --version`. Build enabled providers with `aifactory.NewRouterFromConfig(cfg)` or provider-specific constructors, then perform a chat smoke test with model role defaults and expected output string `Provider smoke`. Add an embedding probe equivalent to `/v1/embeddings` with input `oneday doctor embedding smoke` and model `text-embedding-3-small`, printing exact string `Embedding smoke`. Keep failures non-panicking: return exit code 1 only for hard config parse/validation errors; provider failures should print `FAIL` lines with actionable text.
  </action>
  <verify>
    <automated>go test ./cmd/oneday ./internal/aifactory ./internal/ai/... </automated>
    <automated>go run ./cmd/oneday doctor</automated>
    <automated>grep -R "OneDay doctor\\|Provider smoke\\|Embedding smoke\\|Codex login" cmd/oneday/main.go</automated>
  </verify>
  <acceptance_criteria>
    `go run ./cmd/oneday doctor` prints `OneDay doctor`; source grep finds `Provider smoke`, `Embedding smoke`, `Codex login`, `RAG: enabled`, and `RAG: disabled`; doctor code has no calls to `os.WriteFile`; targeted tests pass.
  </acceptance_criteria>
  <done>`oneday doctor` is implemented as a read-only diagnostic command with OS/tool/provider/env/model/RAG checks and actionable smoke-test output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell to diagnostic probes | Doctor executes local CLIs and optional network/API probes based on local config. |
| `.env` to process environment | Setup may create placeholders; dotenv loader imports values without overwriting exported env vars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | `runDoctor` output | mitigate | Print whether env vars are set, never print actual API key values. |
| T-24-02-02 | Tampering | `runDoctor` | mitigate | Keep doctor read-only; acceptance requires no `os.WriteFile` in doctor path. |
| T-24-02-03 | Denial of Service | provider smoke tests | mitigate | Use existing generation timeout and tiny prompts/embedding input. |
</threat_model>

<verification>
Run targeted command tests, then `go test ./...`. Manually inspect that doctor output does not include secret values.
</verification>

<success_criteria>
`oneday setup` produces safe local config/env scaffolding; `oneday doctor` gives read-only, actionable startup diagnostics including provider and embedding smoke status.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-02-SUMMARY.md`
</output>
