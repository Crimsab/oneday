---
phase: 24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - internal/tui/app.go
  - internal/rag/embeddings.go
  - internal/ai/providers/openai_compat.go
  - config.example.yaml
requirements:
  - RAG-SETUP-01
  - AI-SETUP-03
autonomous: true
must_haves:
  truths:
    - "RAG uses an explicit embedding-capable remote provider or is disabled before gameplay depends on it."
    - "Codex and Claude Code are never selected for embeddings."
    - "No-RAG mode degrades clearly without crashing gameplay."
  artifacts:
    - path: "internal/tui/app.go"
      provides: "runtime RAG provider selection and no-RAG degradation"
    - path: "internal/rag/embeddings.go"
      provides: "embedding call and dimension checks"
    - path: "internal/ai/providers/openai_compat.go"
      provides: "OpenAI-compatible embedding HTTP behavior"
    - path: "config.example.yaml"
      provides: "documented remote embedding defaults"
  key_links:
    - from: "internal/tui/app.go"
      to: "internal/ai/providers/openai_compat.go"
      via: "providers.NewOpenAICompat for embedding provider"
      pattern: "selectEmbeddingProvider|text-embedding-3-small"
---

<objective>
Harden RAG setup so embeddings are deterministic, remote defaults are explicit, optional local fallback is isolated by design, and gameplay degrades clearly when RAG is unavailable.

Purpose: RAG failures should be diagnosed during setup/doctor and should never appear as surprising gameplay crashes.
Output: robust embedding provider selection, smoke-compatible embedding errors, and no-RAG degradation behavior.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
@.planning/phases/12-state-rollback-integrity-and-narrative-persistence/12-03-SUMMARY.md
@.planning/phases/16-streaming-memory-reliability-and-provider-capability-hardening/16-03-SUMMARY.md
@internal/tui/app.go
@internal/rag/embeddings.go
@internal/ai/providers/openai_compat.go
@config.example.yaml

<interfaces>
Current selector in `internal/tui/app.go`:
```go
func selectEmbeddingProvider(cfg config.Config) (embeddingProviderSpec, string)
func embeddingProviderSpecForName(cfg config.Config, name string) (embeddingProviderSpec, string)
```

Required doctor/setup status strings:
- `RAG: enabled, embedding provider:`
- `RAG: disabled, reason: no embedding-capable provider configured`
- `text-embedding-3-small`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make embedding provider selection reusable and explicit</name>
  <read_first>
    - internal/tui/app.go
    - internal/config/config.go
    - internal/ai/provider.go
    - internal/ai/providers/openai_compat.go
  </read_first>
  <files>internal/tui/app.go, internal/ai/providers/openai_compat.go</files>
  <behavior>
    - `selectEmbeddingProvider` returns LiteLLM/OpenRouter only when enabled and configured.
    - Explicit `ai.embedding.provider: codex` cannot validate, and runtime selector still treats Codex as unsupported defensively.
    - Auto selection skips providers with missing `base_url` and providers without embedding capability.
  </behavior>
  <action>
    Keep the existing selector shape unless moving it to a small reusable package is necessary for doctor integration. Add/extend tests around these exact cases: auto selects LiteLLM when LiteLLM is enabled with base URL; auto selects OpenRouter when LiteLLM is disabled and OpenRouter is enabled with base URL; auto returns reason `no embedding-capable provider is enabled` for Codex-only config; explicit `litellm` disabled returns `embedding provider "litellm" is disabled`; explicit `openrouter` missing base URL returns `embedding provider "openrouter" has no base_url configured`. Ensure all user-facing RAG status functions print model `text-embedding-3-small` from config, not hardcoded elsewhere.
  </action>
  <verify>
    <automated>go test ./internal/tui ./internal/ai/providers</automated>
    <automated>grep -R "no embedding-capable provider is enabled" internal/tui internal/ai internal/config</automated>
    <automated>grep -R "text-embedding-3-small" internal/tui config.example.yaml internal/config/config.go</automated>
  </verify>
  <acceptance_criteria>
    Selector tests cover Codex-only, LiteLLM, OpenRouter, disabled provider, and missing base URL cases; source contains `no embedding-capable provider is enabled`; no code path marks Codex as `SupportsEmbeddings: true`; targeted tests pass.
  </acceptance_criteria>
  <done>Embedding provider selection is explicit, test-covered, remote-provider-only for runtime embeddings, and never treats Codex or Claude Code as embedding-capable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clear no-RAG degradation and local fallback design stub</name>
  <read_first>
    - internal/tui/app.go
    - internal/rag/embeddings.go
    - config.example.yaml
    - README.md
    - .planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-CONTEXT.md
  </read_first>
  <files>internal/tui/app.go, internal/rag/embeddings.go, config.example.yaml</files>
  <behavior>
    - Gameplay continues when RAG is disabled because no embedding provider is viable.
    - Logs/status explain `RAG: disabled` with the exact reason before RAG operations are attempted.
    - Local embeddings are not hand-rolled; any local fallback is isolated behind a disabled config/design marker and does not affect remote embedding defaults.
  </behavior>
  <action>
    Update runtime RAG creation so `buildRAG` logs or exposes exact status strings: `RAG: enabled, embedding provider: <name>, model: <model>` and `RAG: disabled, reason: <reason>`. Do not crash if embeddings are unavailable. If adding local fallback configuration, add only a disabled-by-default design field such as `ai.embedding.local.enabled: false` with comments in `config.example.yaml` explaining tradeoffs: binary size, dependency complexity, and cross-platform setup. Do not implement model downloads or a vector database; those are deferred. In `internal/rag/embeddings.go`, make dimension mismatch and HTTP embedding failures return errors that include provider/model context without panics.
  </action>
  <verify>
    <automated>go test ./internal/tui ./internal/rag ./internal/ai/providers</automated>
    <automated>grep -R "RAG: enabled, embedding provider:" internal/tui cmd config.example.yaml</automated>
    <automated>grep -R "RAG: disabled, reason:" internal/tui cmd config.example.yaml</automated>
    <automated>grep -R "local.*embedding\\|embedding.*local" config.example.yaml internal/config/config.go</automated>
  </verify>
  <acceptance_criteria>
    Source grep finds both exact RAG status prefixes; local embedding config, if present, defaults to false and is documented as optional/disabled; `go test ./internal/tui ./internal/rag ./internal/ai/providers` passes; no new dependency or model-download code is added.
  </acceptance_criteria>
  <done>Runtime RAG reports enabled/disabled status clearly, degrades without crashing when unavailable, and documents any local embedding fallback only as disabled design scaffolding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game runtime to embedding provider | Local story text is sent to configured remote embedding endpoint when RAG is enabled. |
| config to RAG pipeline | User-controlled embedding provider/model chooses remote endpoint and dimensionality assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Information Disclosure | remote embeddings | mitigate | Require explicit embedding-capable provider and visible RAG status; do not silently send data through Codex/Claude CLI. |
| T-24-03-02 | Denial of Service | `rag.Embedder` | mitigate | Return contextual errors and disable/degrade RAG instead of crashing gameplay. |
| T-24-03-03 | Tampering | embedding config | mitigate | Validate provider allowlist and dimension/model fields before runtime use. |
</threat_model>

<verification>
Run targeted RAG/provider tests and grep for exact RAG enabled/disabled strings.
</verification>

<success_criteria>
RAG either uses a configured remote LiteLLM/OpenRouter embedding provider with `text-embedding-3-small`, or it is disabled with a clear reason before gameplay depends on it.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ai-provider-onboarding-codex-oauth-and-rag-setup-hardening/24-03-SUMMARY.md`
</output>
