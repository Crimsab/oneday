# Plan 36.1 — Legacy repair and serializable challenge host

- Give RPS draws and memory progress explicit graded outcomes.
- Resolve quick-time from supplied elapsed duration and riddles from normalized
  exact/alias policies rather than local wall clock or substrings.
- Add a versioned reducer host with start, pause, resume, serialize, restore,
  resolve, and deterministic replay lifecycle.
- Preserve authoritative outcomes through TUI adapters and cover regressions.
