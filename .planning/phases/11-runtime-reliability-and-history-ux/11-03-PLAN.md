# Plan 11.3: History Search and Final UX Pass

## Goal

Add an in-story history/search flow and close the remaining discoverability gaps exposed by playtesting.

## Tasks

1. Add `/history` with optional search text to review stored interactions without leaving the current story.
2. Format history results for overlay viewing so turns, speakers, and matches are easy to scan.
3. Update help/discoverability text for save/session/history/challenge behavior.
4. Run focused verification on save/session/history flows and the updated runtime UI.

## Verification

- `/history` shows readable prior interactions from the current session
- `/history <query>` filters to matching turns/messages
- help text reflects the real shortcuts and runtime behavior
- save/session/history flows remain stable under manual playtest
