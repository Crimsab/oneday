export const model_issues = { providerRequired: "Enable at least one provider.", priorityEnabled: "The first provider in the priority chain must be enabled.", modelName: "{{provider}} needs a model name.", providerBaseUrl: "{{provider}} needs a valid service URL.", providerApiKey: "{{provider}} needs an API key.", utility: "Utility model is required.", embedding: "Embedding provider is required.", imageProvider: "Image generation provider is required when auto-generation is enabled.", imageModel: "Image generation model is required when auto-generation is enabled.", mapIcon: "Transparent map icon model is required when auto-generation is enabled.", timeout: "Image generation timeout must be positive.", openClawUrl: "OpenClaw image generation needs a bridge URL.", nativeUrl: "imagegen-bridge needs its native API URL.", baseUrl: "OpenAI-compatible image generation needs a base URL.", apiKey: "OpenAI-compatible image generation needs an API key." } as const;
